name: Import Videos

on:
  workflow_dispatch:
    inputs:

defaults:
  run:
    working-directory: SubtitleConverter

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3
    - name: Run
      run: |
        "${{ secrets.YOUTUBE_CLIENT_ID }}"[-1..-("${{ secrets.YOUTUBE_CLIENT_ID }}".Length)] -join '' | Write-Host
        "${{ secrets.YOUTUBE_CLIENT_SECRET }}"[-1..-("${{ secrets.YOUTUBE_CLIENT_SECRET }}".Length)] -join '' | Write-Host
        
